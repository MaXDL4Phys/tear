# @package _global_

defaults:
  - _self_
  - data: video.yaml # choose datamodule with `test_dataloader()` for evaluation
  - model: video.yaml
  - logger: wandb.yaml
  - trainer: gpu.yaml
  - callbacks: rich_progress_bar.yaml
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml
  - experiment: null

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: null

